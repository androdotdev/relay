# TODO — next update

> Confirmed work items only. Speculative ideas go in discussions, not here.

## Prioritized

- [x] **Decide brand name** — Relay / Pagelish / other. Once decided, rename scope includes:

  - `web/` — dashboard copy, page title, meta tags, favicon
  - `cli/` — `name` in `package.json` and CLI `--version` string
  - `AGENTS.md`, `README.md`, `cli/README.md`, `SECURITY.md`
  - GitHub repo description and topics
  - Domain (if changing)
  *(done — Relay chosen (feat/relay-branding-todo-fixes). Web copy/meta/favicon (layout, hero, dashboard, not-found, icon.svg), cli name `@androff/relay-cli` + `--version` (`relay`, 1.0.0-beta.7), AGENTS.md/README/SECURITY/cli-README, repo description + 8 topics. Domain unchanged — deployment URL still posthtml.vercel.app)*

- [x] **Terminology migration** (can do independently of brand name):

  | File | Changes |
  |---|---|
  | `web/app/api/mcp/lib.ts` | `upload_post → publish_page`, `replace_post → update_page`, descriptions |
  | `cli/src/index.ts` | command names, help text |
  | `web/public/SKILL.md` | agent-facing language |
  | `web/public/llms.txt` | LLM context |
  | Dashboard pages | "Post" → "Page", "Upload" → "Publish" |
  | `web/app/p/[id]/route.ts` | internal comments |
  *(done — MCP `publish_page`/`update_page` (others unchanged); CLI `publish`/`update`, help text pages; SKILL.md + llms.txt; dashboard "Post"→"Page", "Upload"→"Publish" incl. sidebar/buttons/empty states; viewer + proxy copy. Clean cutover — no aliases. API paths `/api/posts`, `/p/:id`, `window.__PH_DATA` unchanged)*

- [x] **DB driver: evaluate WebSocket mode** — `web/db/index.ts` uses `neon-http` (per-query HTTP round-trip). Switch to `@neondatabase/serverless` WebSocket mode for ~150ms warm latency reduction on multi-query operations like `replace_post`. Verify no Vercel Serverless regression.
  *(done — `web/db/index.ts` now defaults to `@neondatabase/serverless` `Pool` + `drizzle-orm/neon-serverless` (WebSocket), sets `neonConfig.webSocketConstructor`; `NEON_TRANSPORT=http` forces the `neon()`/`neon-http` fallback; lazy Proxy init keeps imports safe without `DATABASE_URL`. Typecheck green; connection wiring exercised. Live-Neon latency verification blocked locally — dev `DATABASE_URL` is a `[SENSITIVE]` placeholder; needs a real connection string to finish)*

- [x] **MCP performance: measure and log** — add request duration logging to `route.ts` / `lib.ts` so we can quantify actual P50/P95 before optimizing further.
  *(done — `[mcp] METHOD handled in Xms (auth: header|url token)` on both routes)*
- [x] **API key prefix bug** — CLI stores key as `postbyezbmr…` (bare `post`), but server generates keys with `post_` prefix. The underscore gets lost somewhere in the setup flow. Also check MCP token prefix (`mcp_`).
  *(done — root cause: Better Auth's `defaultKeyGenerator` concatenates prefix with NO separator. Fixed with `customKeyGenerator` in `web/lib/auth.ts` emitting `post_`/`mcp_`; stored `prefix` column stays bare so `k.prefix === "mcp"` lookups keep working)*
- [x] **Markdown upload support** — `--mark` flag on CLI `upload`/`replace`; server converts markdown → HTML:
  *(done — `posts.type` column (`html` default | `markdown`), `web/lib/markdown.ts` (marked) converts at write time so the `html` column always holds rendered HTML; REST + MCP accept `type`; CLI `--mark` on upload/replace; markdown `# heading` becomes the post title; migration `20260806070744_add_post_type`)*
  - `web/db/schema.ts` — add `type` column to `posts` (TEXT, default `"html"`; values `html` | `markdown`). String field, not a boolean — more content types later
  - `web/app/api/posts/route.ts` + `web/app/api/posts/[id]/route.ts` — accept `type` in body; when `markdown`, convert with a markdown→HTML lib before storing (upload and `replace` do the same)
  - `cli/src/index.ts` — `upload`/`replace` check `--mark` and send `type: "markdown"`; skip DOCTYPE validation for markdown; derive title from first `#` heading
  - MCP `upload_post`/`replace_post` — optional `type` arg for parity
  - New migration for the column
- [x] **CLI UX: `post setup` double-pastes the key** — pasted key got written twice (`postbyezbmr…postbyezbmr…AAgL`, 133 chars) → 401. The prompt must capture exactly one read. Also: silent prompt hides keystrokes but piped input path needs the same guard.
  *(done — paste sanitization: first line, exact-duplicate collapse, >100 char warning; `--key` > env > prompt)*
- [x] **CLI: `--title` flag on `upload`/`replace`** — title currently comes only from the HTML `<title>` tag (opaque, undocumented in `--help`); old posts show filename-ish titles in `list`. Flag should win over extracted title.
  *(done — `-t, --title <title>` on `publish`/`update`, documented in `--help`, wins over extracted title; markdown `# heading` no longer dasherized)*
- [x] **CLI: terse errors** — `✗ Error 401: Unauthorized` gives no recovery hint. Include "get a key at <dashboard>" on 401, and context per command (which post/URL failed).
  *(done — 401 hint with dashboard link in `api()`; non-JSON error bodies no longer crash)*
- [x] **Origin isolation friction: proxy route table** — replace string-prefix matching (`path.startsWith("/p/")`) with an explicit route table so sub-paths (`/p/:id/edit`, data admin) are predictable; fixes trailing-slash token bug, stale-token double-`key` param, and edit-link breakage on the posts domain in one pass.
  *(done — `POST_VIEWER_RE` strict single-segment matcher; `/p/foo/bar`, `/p/`, `/p` now get the branded 404 on the posts domain; trailing-slash normalized by the regex; stale `key` drop + token signing unchanged; 3 new tests)*
- [x] **Unify dual rendering paths** — `{{placeholder}}` server interpolation vs `window.__PH_DATA` client injection are two overlapping systems; posts break by picking one accidentally (resume post did). Define one contract: static fields interpolate server-side, dynamic/JS access via `__PH_DATA`; document it in SKILL.md.
  *(done — viewer already applied both unconditionally; contract now explicit in SKILL.md "Rendering contract" + AGENTS.md; `interpolate`/`escapeHtml` extracted to `web/lib/interpolate.ts` with 10 tests; stale docstring claim of unimplemented `{{{raw}}}` triple-brace injection removed)*
- [x] **Remove dead code** — dashboard `UploadPost.tsx` (no importers), `posts-store.ts` `listLoaded` (never read), bulk `DELETE /api/posts`. Keep `verification` table (Better Auth owns its schema). *(done — all three removed)*
- [x] **CLI: plaintext key storage** — `~/.post/config.json` stores the API key in plaintext. At minimum chmod 600 on write; consider keyring integration.
  *(done — API key stored in plaintext in `~/.config/.relay/config.json` at `0600`; OS keyring dependency removed in 1.0.0-beta.10 so the CLI is identical on headless/CI/WSL/tmux; 6 config tests)*

> **Open question — `data` public-by-default:** GET `/api/posts/:id/data` is public unless the post is private, so a post's JSON blob is readable by anyone with the URL. Needed for share-links, but no middle ground exists for "public page, private data". Options: private-post-only data, per-key visibility, or document + encrypt client-side. **Discuss before deciding.**

- [x] **Dashboard: rename "API Keys" section to "Get Started"** — merge Agent Setup Prompt (better UI) + API key generation into one section. Sidebar label + section content.
  *(done — sidebar tab + section heading now "Get Started" (Rocket icon); section subtitle frames key generation + setup prompt as onboarding; AgentSetupPrompt storage text notes config-file storage)*

## Pre-major-update fixes (research audit — 2026-07-31)

> Findings from a 4-agent read-only audit (bugs / security / refactor / launch gaps). Lines verified by reading source; fix directions included, no code changed.

### Must fix — confirmed bugs
- [x] **Dashboard "Save Data" nests post data under a `data` key** — `web/app/post/edit/[id]/page.tsx:95` sends `{data: parsed}`, but PATCH `/api/posts/:id/data` merges the body directly (CLI + MCP send the raw object). `{{placeholder}}` rendering breaks for dashboard saves. Fix: send `JSON.stringify(parsed)`.
  *(done — dashboard saves now send the raw parsed object (no `data` wrapper))*
- [x] **Private posts served with `Cache-Control: public, max-age=300`** — `web/app/p/[id]/route.ts:162-169`; denial paths use `private, no-store` (lines 103-115). Private HTML + `window.__PH_DATA` + `?key=` token cacheable by shared caches. Fix: `private, no-store` when `post.isPrivate`.
  *(done — success path uses `private, no-store` when `post.isPrivate`)*
- [x] **Public GET /api/posts/:id leaks owner `userId`** — `db.select()` returns the full row (`web/app/api/posts/[id]/route.ts:16-24`); same for `/data`. Fix: project only intended fields.
  *(done — GET projects only intended fields)*
- [x] **PATCH /api/posts/:id accepts mixed-type garbage** — AND-chain guard (`[id]/route.ts:59`); `{html: 123, isPrivate: true}` persists non-strings. Fix: per-field type validation.
  *(done — per-field `typeof` validation, 400 on mismatch)*
- [x] **CLI `setup` ignores `POST_API_KEY`** — documented non-interactive flow (`cli/README`) prompts instead of using the env var. Related to the known `post_` prefix bug.
  *(done — setup falls back to `POST_API_KEY`/`POSTHTML_API_KEY` before prompting)*
- [x] **`/p/:id/` trailing slash breaks private access** — `web/proxy.ts:48` signs the token for `id/` (`path.replace("/p/","")`), route compares against normalized `id` → owner gets 403. Fix: strip trailing slash before signing.
  *(done — proxy `POST_VIEWER_RE` normalizes trailing slash before signing)*
- [x] **Proxy appends a second `key` param when a stale token is in the URL** — `web/proxy.ts:52-55`; viewer reads the first (stale) token → 403 for the owner. Fix: replace existing `key`.
  *(done — proxy drops stale `?key=` before signing)*
- [x] **`withError` returns 500 with raw `err.message`** — malformed JSON body → 500 + parser text; DB errors echoed (`web/lib/with-error.ts:7-15`). Fix: 400 on JSON parse errors, generic 500, log server-side.
  *(done — 400 on JSON parse errors, generic 500 otherwise, logged server-side)*
### Must fix — credential lifecycle / security
- [x] **MCP "Generate New URL" leaves the previous token active** — no server-side uniqueness (`web/app/api/keys/route.ts:27-48`); dashboard `generateMcpKey` doesn't revoke (`McpSection.tsx:37-66`). AGENTS.md "replace in place" claim false. Fix: revoke existing `mcp_` key on generate.
  *(done — server-side revoke of previous `mcp_` key on generate)*
- [x] **Private-post capability tokens can't be revoked** — 30-day HMAC, no version (`web/lib/post-token.ts:4-28`); private→public→private keeps old tokens valid. Fix: token-version column on `posts`, rotate on visibility toggle.
  *(done — `posts.token_version` + versioned HMAC, rotated on visibility toggle)*
- [x] **Dashboard-created API keys: no rate limit, no expiry by default** — `POST /api/keys` sets `rateLimitEnabled: false` unless checked; `expiresIn` never sent; plugin default null (`web/lib/auth.ts:22-29`). Decide policy before launch.
  *(done — safe defaults: unlimited, rate limiting on, 24h window)*
### Verify / decide before major update
- [x] **Drizzle migration journal missing** — `web/drizzle/` has 6 migration folders, no `meta/_journal.json`; documented `bun -C web db:migrate` flow likely broken ([INFERENCE] — regenerate journal + verify on scratch DB).
  *(done — inverted: rc.4 requires folder-style migrations WITHOUT a journal; a `meta/_journal.json` throws `MigrationsOutdatedCliError`. Verified all 8 migrations apply in order on fresh Postgres 16 (docker scratch DB); removed broken duplicate `20260806075335_known_major_mapleleaf` (re-added `type`/`token_version` columns, failed on fresh DB); AGENTS.md Migrations section rewritten)*
- [x] **`@better-auth/drizzle-adapter` peer mismatch** — requires `drizzle-orm ^0.45.2`, repo pins `1.0.0-rc.4` (bun.lock:109). Verify compatibility (ideally on Drizzle 1.0 stable).
  *(done — verified compatible: full web suite (40 tests) + typecheck green on `1.0.0-rc.4` with adapter 1.6.23. No 1.0 stable exists (npm latest 0.45.2); downgrading risks rc.4-generated migration/snapshot incompatibility. Re-evaluate when 1.0 stable ships)*
- [x] **API-key quota double-consumed** — proxy re-verifies auth per `/api/posts*` request; every route re-verifies (`web/proxy.ts:67`, routes). Remaining/rate counters may decrement twice per request ([INFERENCE], plugin internals unverified). Decide: drop proxy auth block (keep header strip) or make GETs consume `x-user-id`.
  *(done — proxy is header-strip-only for `/api/posts*`: strips client `x-user-id`, never sets it, never verifies; every route (GET + mutating) calls `getAuthenticatedUserId` exactly once. `proxy.test.ts` rewritten — asserts `fnVerify` NOT called on API paths; 16 proxy tests green)*
- [x] **No rate limiting on public endpoints** — public GETs + failed auth are unthrottled; each costs a Neon round trip (SECURITY.md declares out of scope — revisit before launch).
  *(done — `web/lib/rate-limit.ts`: per-instance in-memory sliding window, default 120 req/IP/60s, tunable `PUBLIC_RATE_LIMIT_MAX`; applied to public GETs (`/api/posts/:id`, `/api/posts/:id/data`, `/p/:id`) with 429. Honest caveat in SECURITY.md: per-instance only — global cap needs an external service)*
- [x] **CI gaps** — `.github/workflows/ci.yml` `build-web` has `continue-on-error: true`; no typecheck anywhere; `vercel.json` `ignoreCommand` skips main. Wire typecheck + fail on build error.
  *(done — typecheck step wired for cli+web; `continue-on-error` removed; web now builds without env via lazy db + guarded auth base URLs)*

### Refactor / polish
- [x] Dead code: `web/app/dashboard/components/UploadPost.tsx` (no importers), `posts-store.ts` `listLoaded` (never read), bulk `DELETE /api/posts` (unused). *(done)*
- [x] API shape drift REST vs MCP: `list_posts` omits `isPrivate`; `upload_post` has no private flag; keys routes use lowercase errors + a second auth idiom (`web/app/api/keys/route.ts`).
  *(done — MCP: `isPrivate` in list/upload/replace/get_post meta; keys error casing normalized to "Unauthorized"; session-only auth posture kept deliberately)*
- [x] De-dup `BASE_URL` (×6, diverging defaults) and `MAX_HTML_SIZE` (×3) into shared modules.
  *(done — `web/lib/constants.ts`; consumed by mcp, posts routes, keys route; CLI keeps its own config-based BASE_URL)*
- [x] CLI polish: `replace` prints "[public]" for an unchanged private post; `--private --public` conflict resolves silently; `api()` crashes on non-JSON error bodies.
  *(done — PATCH echoes `isPrivate`; conflicting flags error out; `api()` handles non-JSON bodies + 401 hint)*
- [x] Dashboard/edit UX: fetch failures silently swallowed (`dashboard/page.tsx:115-165`); edit page can spin forever on load error (`post/edit/[id]/page.tsx:52-63`).
  *(done — dismissible error banner on dashboard for load/delete/toggle/revoke failures; edit page catches network errors with a "couldn't load" screen + back link instead of an infinite spinner)*
- [x] `/api/mcp` is POST-only but documented as GET/POST (`AGENTS.md`) — add GET or fix docs.
  *(done — GET added to the header-auth route mirroring POST (same duration logging); URL-token route already had GET)*
- [x] Docs refresh: AGENTS.md (migration count "2" → 7 + db:push note, env list + `POSTS_DOMAIN`/`POST_TOKEN_SECRET`, design-token table → Catppuccin, bulk DELETE row removed, MCP auto-revoke), cli/README env-priority contradiction, `.env.example` stale `ptd` section + commented `POST_TOKEN_SECRET`. *(done)*

## Done (this session)

- [x] CLI README — full docs for every command
- [x] Silent stdin prompt (`readSilent` in `cli/src/index.ts`)
- [x] Proxy test mock fix (`new NextResponse` support)
- [x] MCP tool descriptions updated to explain `{{placeholder}}` → data relationship
- [x] SKILL.md / llms.txt restructured with "templates + data" as core concept
- [x] CLI version bumped to `0.9.0`, tag `cli-v0.9.0` created locally
- [x] Security review — no action needed
